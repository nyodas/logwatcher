language: go

sudo: required
dist: trusty

go:
  - 1.7.5

os:
  - linux

install:
  - go build
  - go get -u github.com/golang/dep/cmd/dep

script:
  - dep ensure
  - go test -cover -v $(go list ./... | grep -v vendor/)
